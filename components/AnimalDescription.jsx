const React = require('react');
const Layout = require('./Layout');

function AnimalDescription({ title, animals, gallery }) {
  return (
    <Layout title={title} animals={animals} gallery={gallery}>
      <div>

      </div>
    </Layout>
  );
}

module.exports = AnimalDescription;
